const rectangleArea = (width, height) => {
    let area = width * height;
    return area;
};


// Another Example

const plantNeedsWater = (day) => {
    if (day === 'Wednesday') {
        return true;
    } else {
        return false;
    }
};